<?xml version="1.0" encoding="windows-1251" ?>
<?xml:stylesheet type="text/xsl" href="common/layout.xsl" ?>

<lesson title="Урок №6" next="section5.xml" prev="section3.xml" summ="1">
<h2>Пример программы для нахождения суммы отрицательных элементов массива. </h2>
<h5>Постановка задачи.</h5>
<p>
Написать программу, которая находит сумму всех отрицательных значений в массиве.
</p>
<h5>Код реализации.</h5>
<example>
#include &lt;iostream&gt;
using namespace std;
void main ()
{
	//определение размера массива
	const int size=5; 

	//создание и инициализация массива данными
	int ar[size]={23,-11,9,-18,-25};

	//переменная для накопления суммы
	int sum=0;

	//цикл, перебирающий по порядку элементы массива
	for (int i=0;i&lt;size;i++)
	{
		if(ar[i]&lt;0) //если значение элемента отрицательное (меньше нуля)
			sum+=ar[i]; //добавить его значение к общей сумме			
	}

	//показ значения суммы на экран
	cout&lt;&lt;"Sum = "&lt;&lt;sum&lt;&lt;"\n\n";
}
</example>

<h5>Комментарий к коду.</h5>
<p>1. Цикл поочередно перебирает элементы от 0 до size. При этом size не входит в проверяемый диапазон, т. к. индекс последнего элемента size-1.</p>
<p>2. На каждой итерации цикла происходит проверка содержимого элемента на отрицательное значение.</p>
<p>3. Если значение меньше нуля, оно прибавляется к сумме.</p>
<hr/>
<p>Как видите, работа с массивом очень похожа на анализ какого-то диапазона. Только, в данном случае, минимальная граница диапазона - 0, а максимальная - определяется количеством элементов в массиве.</p>
</lesson>
