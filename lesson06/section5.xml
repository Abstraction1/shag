<?xml version="1.0" encoding="windows-1251" ?>
<?xml:stylesheet type="text/xsl" href="common/layout.xsl" ?>

<lesson title="Урок №6" next="hometask.xml" prev="section4.xml" summ="1">
<h2>Пример программы для нахождения минимального и максимального элементов массива.</h2>
<h5>Постановка задачи.</h5>
<p>
Написать программу, которая находит минимальное и максимальное значение в массиве и показывает их на экран. 
</p>
<h5>Код реализации.</h5>
<example>
#include &lt;iostream&gt;
using namespace std;
void main ()
{
	// определения количества элементов массива
	const int size=5;

	// создание и инициализация массива
	int ar[size]={23,11,9,18,25};
	
	int max=ar[0];	// пусть 0 элемент максимальный
	int min=ar[0];	// пусть 0 элемент минимальный

	//цикл перебирает элементы массива начиная с 1-ы
	for (int i=1;i&lt;size;i++)
	{
		//если текущий элемент меньше, чем минимум
		if(min&gt;ar[i])

			//перезаписать значение минимума
			min=ar[i]; 
		
		//если текущий элемент больше, чем максимум
		if(max&lt;ar[i])

			//перезаписать значение максимума
			max=ar[i];
	}

	// вывод результата на экран
	cout&lt;&lt;"Max = "&lt;&lt;max&lt;&lt;"\n\n";
	cout&lt;&lt;"Min = "&lt;&lt;min&lt;&lt;"\n\n";	
}
</example>

<h5>Комментарий к коду.</h5>
<p>1. Для начала, выдвигаем предположение, что минимальным является элемент массива с индексом 0.</p>
<p>2. Записываем значение элемента с индексом 0 в переменную min.</p>
<p>3. Затем, для того, что бы либо подвердить, либо опровергнуть этот факт, перебираем все элементы массива начиная с элемента с индексом 1 в цикле.</p>
<p>4. На каждой итерации цикла, сравниваем предполагаемый минимум с текущим элементом массива(элемент с индексом i).</p>
<p>5. Если встречается значение меньше, чем предполагаемый минимум - значение min перезаписывается на меньшее найденное значение и анализ продолжается.</p>
<p>Все вышеописанные действия справедливы и для максимума, только осуществлять необходимо поиск большего значения.</p>
<hr/>
<p>Теперь, когда вы знакомы с массивами и рассмотрели несколько примеров, пора сделать что-то самим. Желаем удачи в прохождении теста и выполнении домашнего задания.</p>
</lesson>
