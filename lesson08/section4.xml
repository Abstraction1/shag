<?xml version="1.0" encoding="windows-1251" ?>
<?xml:stylesheet type="text/xsl" href="common/layout.xsl" ?>

<lesson title="Урок №8" next="section5.xml" prev="section3.xml" summ="1">
<h2>Область видимости. Глобальные и локальные переменные. </h2>
<h3>Область видимости.</h3>
<p>
Любые фигурные скобки в программном коде образуют, так называемую область видимости, это означает, что переменные, объявленные внутри этих скобок будут видны только внутри этих скобок. Другими словами, если к переменной, созданной внутри функции, цикла, if и так далее обратится из другого места программы, то произойдет ошибка, так как после выхода из фигурных скобок эта переменная будет уничтожена.
</p>
<example>
int a=5;
if(a==5){
	int b=3;
}
cout&lt;&lt;b; // ошибка! b не существует
</example>

<h3>Глобальные и локальные переменные</h3>	 
<p>Согласно правилам области видимости - переменные делятся на два вида – <strong>локальные</strong> и<strong> глобальные</strong>.</p>
<p> Локальные переменные создаются внутри какого – нибудь  отрезка кода, что это значит для программы, мы уже знаем.</p>
<p>
Глобальные переменные создаются вне всяких областей видимости. Преимущественно до функции main().<strong> Такая переменная видна в любом месте программы</strong>. По умолчанию глобальные переменные в отличии от локальных инициализируются 0. И, главное, те изменения, которые происходят с глобальной переменной внутри функции, при выходе из последней сохраняются.
</p>						

<tip start="Примечание">Запомните - если есть, например, глобальная переменная под названием а, а внутри функции определяется переменная под тем же именем, то тогда внутри функции будет использоваться переменная, объявленная внутри этой функции. Поэтому избегайте применения имен переменных, которые незримо уже используются во внешних областях видимости. Этого можно достигнуть, вообще избегая использования в программе одинаковых идентификаторов.
</tip>
<example>
int a=23; // глобальная a
void main(){
	int a=7;// локальная a
	cout&lt;&lt;a; // 7, используется локальная
}

</example>

</lesson>
