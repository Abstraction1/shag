<?xml version="1.0" encoding="windows-1251" ?>
<?xml:stylesheet type="text/xsl" href="common/layout.xsl" ?>

<lesson title="Урок №7" next="section3.xml" prev="section1.xml" summ="0">	  

<h2>Использование генератора случайных чисел.</h2>
<p>
Давайте рассмотрим пример использования генератора случайных чисел, а именно заполнение массива случайными числами:
</p>

<example>
#include&lt;iostream&gt;
#include&lt;stdlib.h&gt; // в этом файле содержатся функции rand() и srand()
#include&lt;time.h&gt; // в этом файле содержится функция time()
using namespace std;
void main()
{
	srand(time(NULL));
	int array[10];
	for (int i=0;i&lt;10;i++) 
	{
		// генерация случайного числа и запись его в текущий элемент массива
		array[i]=rand()%100;

		// показ значения элемента на экран
		cout&lt;&lt;array[i]&lt;&lt;”\n”;
	}
}

</example>

<p>
1. В приведенном выше примере на экран будет выведен массив из 10 элементов, заполненный случайными числами.
</p>
<p>
2. На каждой итерации цикла генерируется новое случайное число.
</p>
<p>
3. При каждом запуске программы массив будет заполнен по разному, благодаря строке srand(time(NULL));
</p>
<p>
4. Числа располагающиеся, в массиве будут варьироваться в диапазоне от нуля до 100, так как результат генерации делится на 100 по модулю.
</p>
<hr/>
<p>Как видите, генератор случайных чисел прост в обращении, и теперь у вас в руках есть оружие, которое позволит вам не только тестировать программы, не вводя данные с клавиатуры, но и создавать примитивный искусственный интеллект.</p>
</lesson>
